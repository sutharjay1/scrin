import React from 'react';
import { Text, View } from 'react-native';

const Liked = () => {
	return (
		<View className="flex-1 items-center justify-center bg-background">
			<Text className="font-bold text-card">Liked</Text>
		</View>
	);
};

export default Liked;
