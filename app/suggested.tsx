import React from 'react';
import { Text, View } from 'react-native';

const Suggested = () => {
	return (
		<View className="flex-1 items-center justify-center bg-background">
			<Text className="font-bold text-card">Suggested</Text>
		</View>
	);
};

export default Suggested;
