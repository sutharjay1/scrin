import React from 'react';
import { Text, View } from 'react-native';

const Library = () => {
	return (
		<View className="flex-1 items-center justify-center bg-background">
			<Text className="font-bold text-card">Library</Text>
		</View>
	);
};

export default Library;
