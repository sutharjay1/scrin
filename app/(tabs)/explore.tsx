import React from 'react';
import { Text, View } from 'react-native';

const Explore = () => {
	return (
		<View className="flex-1 items-center justify-center bg-background">
			<Text className="font-bold text-card">Explore</Text>
		</View>
	);
};

export default Explore;
